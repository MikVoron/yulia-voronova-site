-- Добавляет колонку time_label для текстового представления времени приготовления.
-- time_min продолжает использоваться как целое (нижняя граница диапазона) для фильтров/сортировки.
-- time_label — то, что пользователь дал в исходном тексте («45–50 минут», «1 час 20 минут»).

ALTER TABLE recipes ADD COLUMN IF NOT EXISTS time_label VARCHAR(60);
