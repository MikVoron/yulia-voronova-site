# План безопасности и надёжности SmartPlate

Статус: обновлён 2026-09-14. Это рабочая последовательность: один крупный этап
за раз, с checkpoint, проверкой и понятным откатом. Она не заменяет продуктовый
план и не требует изменений дизайна.

## Выполнено

- SSH-доступ root отключён; рабочий доступ — `smartplate-admin` по ключу и с
  парольным `sudo`. Аварийный доступ остаётся через консоль Timeweb.
- API-процесс `smartplate-api` работает от UID/GID `997`, без Linux capabilities.
  PM2-менеджер пока остаётся root, но больше не является пользователем самого API.
- Проверены локальный зашифрованный backup БД, cron и мониторинг; перед рискованными
  изменениями созданы root-only checkpoints с архивом файлов и custom DB dump.
- Обновлены production-зависимости: Fastify `5.12.3`, Nodemailer `9.1.1`,
  fast-uri `3.1.6`, find-my-way `9.9.0`; `npm audit --omit=dev` = 0.
- После обновления проверены local/public `/health`, PM2, UID/GID `997` и отсутствие
  активного rollback timer.
- Для закрытых рецептов API уже не выдаёт гостю ingredients, steps и note;
  приватные API-маршруты возвращают 401 без сессии.
- CSP для script выполнен: inline scripts и inline event handlers удалены. Переход
  от inline styles — отдельная, не срочная задача.

## Приоритет 1 — сделать до следующего рискованного backend-релиза

### 1. Постоянный безопасный сценарий API-релиза

Причина: старые инструкции с `ssh root` больше не актуальны, а одноразовый
release helper показал, что права файлов после `npm ci` критичны для API.

Промежуточный статус 2026-09-11:

- [x] Актуализирован release-checklist (`b79c564`).
- [x] Подготовлен [прототип протокола 0.1.0](../server/ops/release/README.md):
  manifest, read-only планировщик и тесты отказов/переходов состояний.
- [x] Подготовлен отдельный Linux-стенд для искусственного процесса, offline-копий,
  реального flock, таймеров и аварийных обрывов. Предварительные тесты на VPS
  без sudo: 21/21.
- [ ] Реализовать Linux-адаптер: root-owned staging, глобальный lock, атомарные
  записи состояния и восстановление сохранённых модулей без npm/сети.
- [x] Выполнена root/systemd репетиция на искусственном процессе: 13/13 сценариев,
  включая реальный таймер, аварийные обрывы, повреждённый backup и гонку команд.
  Run: `run-4fcb2d7b5531a74c`, код `38a9d12`. По выводу прогона production
  PID/файлы/health не изменились. После прогона отдельно проверены: 0 загруженных
  units и 0 timers этого run, API PID `2918793`, PM2 PID `762`, публичный health/DB OK.
- [x] Согласован протокол 0.2.0: устойчивое решение до снятия таймера, повторные
  проверки перед commit, отдельная повторяемая уборка. Локально protocol-тесты
  17/17; версия состояния 2, production-исполнения по-прежнему нет.
- [x] Подготовительная PM2-репетиция от UID 1000: отдельный PM2_HOME,
  сохранение/resurrect/возврат конфигурации и отказ при повреждённом checkpoint.
  Run `/tmp/sp-pm2-rh-qtuXZ9/`: 5/5, production baseline не изменился,
  тестовый сервис остановлен. Это ещё не интеграция протокола с PM2/UID 997.
- [x] Подготовлен единый журнал state/recovery с намерением arming и отдельной
  отменой/уборкой (`control-envelope.cjs`). Новые тесты на VPS: 18/18, включая
  реальные atomic writes/fsync/flock/SIGKILL; PM2/timer-evidence пока модельное.
- [x] Подготовлен отдельный root PM2/systemd-стенд (`integrated-rehearsal.cjs`):
  root-owned журнал, искусственные процессы UID997, offline dump/модули и 10
  сценариев. Staging `integrated-rehearsal-20260911-i5M6bz`, preflight OK,
  непривилегированные тесты VPS 24/24. Пользователь выполнил root-прогон
  `run-0314706e92e8e57f`: 10/10 сценариев и production unchanged, но финальная уборка
  bootstrap отказала с `INTEGRATED_PROTECTED_COPY`. В репозитории исправлен вызов
  через защищённую копию без ослабления проверки; 3 новых регрессионных теста,
  общий локальный набор 61 passed / 14 Linux-only skipped. Исправленный bootstrap
  прошёл последующий root-прогон `run-9416adc255eb91cb` с тремя маркерами успеха;
  его root-only результаты переданы пользователем и подтвердили успех и уборку
  (подробности ниже). После команды cleanup первого run
  в 17:42:06 UTC независимо подтверждены 0 units / 0 timers этого run и прежние
  API/PM2 PID, health ok. Пользователь отдельно передал root-only результаты:
  `passed: true`, `cases: 10`, `productionUnchanged: true`, `cleanupComplete: true`,
  а в `resources-stopped.json` — `complete: true`. Уборка текущего стенда завершена.
  Подробности — в README helper.
- [x] Добавлен fixture PM2/startup-адаптер и пройдена отдельная репетиция
  протокола 0.2.0 с модельной сменой boot ID: 10/10, подробности ниже.
- [ ] Добавить production evidence-адаптер, root-owned boot service и проверить
  recovery после настоящей перезагрузки только после отдельного согласования.
- [ ] Установить проверенный helper и пройти первый согласованный малый релиз.

Прототип не выполняет deploy. Старый временный helper не становится универсальным
после переноса файлов. Пункт 1 целиком пока не завершён.

Изолированная cold-start репетиция `run-e0824dc6a8402e72` завершена.
Пользователь передал успех всех 10 сценариев, production unchanged и финальный
маркер остановки ресурсов. Root-only отчёты подтверждают `passed: true`,
`coldStartRecoveryTested: true`, `cleanupComplete: true` и отдельное
`complete: true`. В 11:51:59 UTC независимо
подтверждены 0 units / 0 timers run, прежние API PID 2918793 (UID/GID997), PM2 PID762,
PostgreSQL PID721 и публичный health status/db ok. Повторять запуск не нужно.
Режим `--boot-rehearse` использует отдельный PM2 и модельную смену boot ID.
Непривилегированные VPS-тесты 87/87, preflight OK;
локально 73 passed / 14 Linux-only skipped. Комплект из восьми helpers и три
изменённых/новых теста сверены по SHA-256. Staging
`/home/smartplate-admin/cold-start-20260914-jgHd8A/release/`; точная команда и
границы доказательства — в [README](../server/ops/release/README.md).
Даже успех этой репетиции не проверит загрузку ОС и порядок старта системных служб:
`osBootTested: false`; production boot service не установлен.

Следующий инфраструктурный этап: спроектировать production evidence-адаптер и
root-owned boot recovery service, проверить установку и порядок systemd на отдельном
стенде, и только затем согласовывать реальную перезагрузку. Этот этап требует
Astra High; в текущую cold-start репетицию он не входил.

Сквозная проверка исправленного bootstrap завершена в `run-9416adc255eb91cb`.
Новый staging `integrated-bootstrap-20260911-Tcp2v8` проверен:
SHA-256 всех 13 файлов сверены, fingerprint семи helpers совпал, preflight OK,
непривилегированные VPS-тесты 27/27; локально 61 passed / 14 Linux-only skipped.
Пользователь выполнил новый sudo-прогон: 10/10, production unchanged и
`INTEGRATED_FIXTURE_RESOURCES_STOPPED`, без ошибки helper. В 18:14:32 UTC
независимо подтверждены 0 units / 0 timers нового run, API PID 2918793,
PM2 PID 762, публичный health status/db ok. Повторять запуск не нужно.
Пользователь отдельно передал root-only файлы нового run: `passed: true`, `cases: 10`,
`fixtureProtocolIntegrated: true`, `fixtureUid997Tested: true`, `productionUnchanged: true`,
`cleanupComplete: true`; `resources-stopped.json` содержит `complete: true`.
`auditZero`/`publicHealth` остаются модельными, `productionExecutionEnabled: false`,
`osBootTested: false`. Успех стенда не является допуском к production-релизу.
Уборка `run-0314706e92e8e57f` завершена:
root-only результаты переданы пользователем, в 17:42:06 UTC независимо подтверждены
0 units / 0 timers, API PID 2918793, PM2 PID 762, health ok. Повторять cleanup не нужно.
Boot recovery и все проверки реального приложения — последующие
обязательные этапы до допуска к production. Рабочий PM2_HOME не использовать.

Локальная политика boot/clock recovery исправлена после проверки:
`server/ops/release/recovery-policy.cjs` явно возобновляет `rolling_back`,
привязывает запись схемы 2 к релизу/manifest и проверяет оба десятиминутных окна.
Перевод часов назад больше не блокирует цепочку rollback → restored → cleanup;
нормализация времени разрешена только этим операциям. Проверки времени для
подтверждения не ослаблены. Recovery-тесты: 16/16, общий Windows-прогон
с PM2 contract-тестами: 41 passed, 7 Linux-only skipped; на VPS 48/48.
Unit-тесты не запускают PM2/API; отдельный PM2-прогон описан выше.
Единая запись state/recovery и отмена незавершённого arming проверены 2026-09-11
в отдельном файловом стенде: новые тесты VPS 18/18, общий локальный набор 52 passed,
14 Linux-only skipped. Прикладное evidence в этом стенде модельное. Подключение
журнала к root-owned хранилищу, реальным таймерам, PM2 и UID997 реализовано в новом
fixture-стенде; 10 сценариев root PM2/systemd прошли по выводу пользователя,
тестовые units/timers остановлены; переданные пользователем root-only результаты
подтвердили успех сценариев и уборки первого run. Исправленный bootstrap прошёл
отдельный root-прогон с финальным маркером уборки; root-only файлы переданы пользователем
и подтвердили успех и завершённую уборку.
Интегрированная boot/PM2-репетиция пока не подтверждена. Пункт 1 остаётся открытым.

Сделать:

1. Переписать `docs/smartplate-release-checklist.md` под `smartplate-admin`,
   парольный `sudo`, `PM2_HOME=/root/.pm2` и API от UID 997.
2. Вынести проверенный release/rollback helper из временной папки в поддерживаемое
   место с версией и тестом синтаксиса.
3. Перед каждым API-релизом: checkpoint, сборка зависимостей отдельно от live с
   читаемыми для UID 997 правами, проверка их загрузки и тесты, timer rollback,
   local/public health, затем явное confirm. Откат возвращает сохранённые модули
   с прежними правами без повторного `npm ci` и без зависимости от сети.

Критерий готовности: следующий малый API-релиз проходит по документу без ручных
команд root и без временных сценариев.

## Приоритет 2 — сделать в ближайшие 1–2 недели

### 2. Реальная проверка восстановления backup

2026-09-12 пользователь выбрал этот пункт следующим; boot-recovery пока отложен.
Начата read-only подготовка: инструменты и ресурсы VPS проверены, выбран encrypted
архив от 2026-09-12 03:00 (1585086 bytes). Пользователь передал SHA-256: серверный
backup.sh совпал с локальным кодом; hash архива закреплён. Подготовлен отдельный
download/decrypt/TOC helper, staging `backup-prepare-20260912-hbvNrb`; preflight OK,
VPS-тесты 6/6 и hash parity обоих файлов. Пользователь выполнил этот helper:
`run-e76d043900822355`, B2-копия совпала по hash, затем wrapper отказал с `PREPARE_TOC`.
Воспроизведён вероятный источник ошибки: EPIPE при успешном раннем выходе pg_restore --list.
Подготовлено узкое исправление и новый staging `backup-recheck-20260912-v2JX8E`,
preflight OK, VPS-тесты 9/9. Пользователь выполнил recheck без B2/SQL:
`run-623e21cd8157fdbf`, копия проверена, TOC status=0/error=EPIPE, 32 public-таблицы,
`DATABASE_RESTORE_NOT_RUN`. В 18:46:12 UTC PostgreSQL main online, API/PM2 PID прежние,
публичный health status/db ok. Root-only result.json передан пользователем и сверён:
271 entries, 32 publicTables/publicTableData, decrypted SHA-256 закреплён.
13 сентября подготовлен helper восстановления и отдельный synthetic-only режим.
14 сентября пользователь выполнил systemd-репетицию `run-8acaf76e2ed8eb7f`: все маркеры
успеха и уборки присутствуют. Независимо подтверждены inactive unit, отсутствие runtime,
совпадение hash защищённого кода, прежние production PID и health ok.
Root-only отчёты переданы и сверены: DynamicUser UID 62120, изоляция подтверждена,
2 таблицы / 4 строки, constraintTest=true, invalidIndexes=0, полная уборка.
В 06:43:58 UTC preflight и hash защищённой копии повторно проверены; подготовлена команда
реального restore в новый временный PostgreSQL. Пользователь выполнил её:
`run-5ddf3bdfa61a59f5`, все маркеры успешны, `RESTORE_DATABASE_OK tables=32`.
В 06:46:34 UTC независимо подтверждены отсутствие обоих runtime, inactive временные unit,
прежние API/PM2/PostgreSQL PID и health ok. Итоговый result.json и оба cleanup-отчёта
переданы пользователем и сверены: databaseRestored=true, 32 таблицы, 17369 строк,
29 непустых таблиц, invalidIndexes=0, DynamicUser UID 61245, изоляция и уборка подтверждены.
Закреплённые encrypted/decrypted hashes совпали, productionUnchanged=true.
Подробности и безопасные границы: [план restore-drill](smartplate-backup-restore-drill.md).

Подтверждены скачивание выбранной B2-копии, совпадение encrypted SHA-256, расшифровка
и чтение TOC (результат передан пользователем; result.json сверён).
**Ограниченный restore-drill выбранного архива завершён 2026-09-14.**
Восстановление исходных владельцев/ACL и бизнес-сценарии приложения не входили в проверку.
Полная аварийная готовность сервиса и boot-recovery этим прогоном не подтверждены.

Критерий готовности: без обращения к production DB восстановлены данные в
одноразовую БД, проверены таблицы и удалена тестовая БД. Повторять ежемесячно и
после изменения backup-скрипта.

### 3. Решение по видео закрытых рецептов

Известный риск: прямые URL видео у части закрытых рецептов могут быть удобны для
пересылки вне подписки. Это вопрос бизнес-правила, а не косметическая правка.

Сначала выбрать политику:

- видео можно свободно смотреть и оно служит анонсом рецепта; или
- видео — часть платного контента.

Если видео платное: отдельный этап с авторизованной выдачей/подписанными URL,
проверкой доступа и тестами гостя, trial и paid-пользователя.

## Приоритет 3 — после этого, без срочности

### 4. Внешний мониторинг

Локальный мониторинг полезен, но не видит часть сетевых сбоев. Добавить внешнюю
HTTP(S)-проверку `plate.voronova.online` и `api.voronova.online/health` с
уведомлением в Telegram/email. Это задача надёжности, не блокер текущего API.

### 5. Отдельная миграция PM2-менеджера от root

API-child уже изолирован от root. Перевод самого PM2 и systemd unit на отдельного
пользователя — отдельный высокий инфраструктурный риск: нужна репетиция boot,
monitor и rollback. Не смешивать его с продуктовым релизом.

### 6. CSP для стилей и XSS-регрессия

Script-политика уже строгая. Позже по страницам убрать оставшиеся inline styles,
только затем ужесточать `style-src`. Добавить регулярные XSS-тесты для feedback,
отзывов и payment comments.

### 7. Регулярная гигиена

- раз в месяц: `npm audit --omit=dev`, проверка backup и monitor;
- перед крупными обновлениями: ревизия Node/ОС и maintenance window;
- при масштабировании до нескольких API-процессов: Redis store для rate-limit.

## Не является срочным сейчас

- Sentry/status page — полезны для наблюдаемости, но не закрывают текущий риск;
- Redis не нужен при одном API-процессе;
- миграция inline styles CSP не должна мешать продуктовым задачам;
- не трогать `tmp/threads-og-audit/` в рамках этого плана.

## Текущая точка отката

Последний подтверждённый checkpoint перед обновлением зависимостей:
`/root/smartplate-rollbacks/api-nonroot-pre-20260910-064004-mAy9Tg`.

Не выполнять rollback из него автоматически: он содержит снимок файлов и БД до
релиза. Для инцидента сначала определить точную неисправность и восстанавливать
минимально необходимый компонент.
